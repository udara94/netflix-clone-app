class DurationManager {
  static Duration duration300ms = const Duration(milliseconds: 300);
  static Duration duration3s = const Duration(seconds: 3);
  static Duration duration1s = const Duration(seconds: 1);
}
